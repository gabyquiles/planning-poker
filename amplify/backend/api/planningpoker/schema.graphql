#type Issue @model {
#    id: ID!
#    name: String
#    votes: [Vote]
#}

type User @model {
    cognitoId: ID!
    id: ID!
    email: String!
}

type Vote @model{
    id: ID!
    value: String
    author: String!
    show: Boolean!
}

type Session @model {
    id: ID!
#    users: [User]
#    issues: [Issue]
    owner: String
}
